---
import BaseLayout from '../layouts/BaseLayout.astro';
import { emphasizeLatinTerms, resolveLocale, useTranslations } from '../i18n';
import { site } from '../lib/site';

const locale = resolveLocale(Astro.currentLocale);
const t = useTranslations(locale);
---

<BaseLayout>
	<section class="hero fade-in" aria-label={t.home.skylineAlt} style={`--hero-skyline: url('${site.ottawaBanner}')`}>
		<div class="hero__marks">
			<img
				class="hero__lung"
				src={site.lungLogo}
				alt={t.home.lungAlt}
				width="888"
				height="790"
			/>
			<img
				class="hero__wordmark"
				src={site.logo}
				alt={t.home.wordmarkAlt}
				width="1024"
				height="290"
			/>
		</div>
		<div class="hero__skyline" aria-hidden="true"></div>
	</section>

	<section class="home-bio" aria-labelledby="home-bio-heading" data-scroll-fade>
		<h2 id="home-bio-heading" class="sr-only">{t.home.aboutHeading}</h2>
		<div class="home-bio__layout">
			<img
				class="home-bio__portrait"
				src={site.headshot}
				alt={t.home.headshotAlt}
				width="203"
				height="203"
			/>
			<p set:html={emphasizeLatinTerms(t.home.bio)} />
		</div>
	</section>

	<noscript>
		<style>
			.home-bio {
				opacity: 1 !important;
				transform: none !important;
			}
		</style>
	</noscript>

	<script is:inline>
		/**
		 * Reveals the homepage biography with a translate-up fade when it
		 * intersects the viewport. Inline so it runs on the static page
		 * without waiting for a bundled island. Reduced-motion visitors
		 * see the paragraph immediately.
		 */
		(function initHomeBioFade() {
			var bio = document.querySelector('[data-scroll-fade]');
			if (!bio || bio.getAttribute('data-fade-ready') === 'true') return;
			bio.setAttribute('data-fade-ready', 'true');

			var reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
			if (reduceMotion || typeof IntersectionObserver !== 'function') {
				bio.setAttribute('data-visible', 'true');
				return;
			}

			bio.classList.add('is-pending');

			var observer = new IntersectionObserver(
				function onIntersect(entries) {
					for (var i = 0; i < entries.length; i += 1) {
						if (!entries[i].isIntersecting) continue;
						bio.setAttribute('data-visible', 'true');
						bio.classList.remove('is-pending');
						observer.disconnect();
						return;
					}
				},
				{ root: null, threshold: 0, rootMargin: '0px 0px -12% 0px' },
			);

			observer.observe(bio);
		})();
	</script>
</BaseLayout>

<style>
	.hero,
	.home-bio {
		--hero-logo-height: min(30vh, 13.5rem);
	}

	.hero {
		/* Pull out of .site-main so the skyline can span the viewport. */
		--hero-skyline-gap: 3.5rem;
		width: 100vw;
		max-width: 100vw;
		margin-inline: calc(50% - 50vw);
		margin-top: calc(-1 * var(--space-xl));
		min-height: calc(100svh - 6.5rem);
		display: flex;
		flex-direction: column;
		justify-content: flex-start;
		align-items: center;
		padding: var(--space-lg) 0 0;
		background-color: #ffffff;
	}

	.hero__marks {
		display: flex;
		flex-direction: row;
		flex-wrap: nowrap;
		align-items: center;
		justify-content: center;
		gap: clamp(1.25rem, 4vw, 3rem);
		width: min(100% - 2rem, var(--max-width-wide));
		margin-bottom: var(--hero-skyline-gap);
		background: transparent;
		flex: 0 0 auto;
	}

	.hero__skyline {
		flex: 1 1 auto;
		align-self: stretch;
		width: 100%;
		min-height: 12rem;
		background-image: var(--hero-skyline);
		/* Width-first so the Peace Tower is not cropped; white sky fills above. */
		background-size: 100% auto;
		background-position: center bottom;
		background-repeat: no-repeat;
	}

	.hero__lung,
	.hero__wordmark {
		height: var(--hero-logo-height);
		width: auto;
		object-fit: contain;
		background: transparent;
	}

	.hero__lung {
		flex: 0 0 auto;
	}

	.hero__wordmark {
		flex: 0 1 auto;
	}

	.home-bio {
		max-width: min(100% - 2rem, 52rem);
		margin-inline: auto;
		padding: var(--space-xl) 0 var(--space-2xl);
	}

	.home-bio__layout {
		display: flex;
		flex-direction: row;
		flex-wrap: wrap;
		align-items: center;
		justify-content: center;
		gap: clamp(1.25rem, 3vw, 2rem);
	}

	.home-bio__portrait {
		flex: 0 0 auto;
		width: min(var(--hero-logo-height), 203px);
		height: min(var(--hero-logo-height), 203px);
		object-fit: cover;
		object-position: center top;
		border-radius: 50%;
		border: 1px solid var(--color-line);
		background-color: var(--color-paper-deep);
	}

	.home-bio.is-pending {
		opacity: 0;
		transform: translateY(2.75rem);
	}

	.home-bio[data-visible='true'] {
		opacity: 1;
		transform: translateY(0);
		transition:
			opacity 800ms var(--ease-out),
			transform 800ms var(--ease-out);
	}

	.home-bio p {
		flex: 1 1 22rem;
		margin: 0;
		max-width: 42rem;
		font-size: 1.08rem;
		line-height: 1.7;
		text-align: left;
	}

	.home-bio i {
		font-style: italic;
	}

	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border: 0;
	}

	@media (max-width: 900px) {
		.hero,
		.home-bio {
			--hero-logo-height: min(20vh, 9rem);
		}
	}

	@media (max-width: 640px) {
		.home-bio__layout {
			flex-direction: column;
		}

		.home-bio p {
			text-align: center;
		}
	}

	@media (max-width: 560px) {
		.hero,
		.home-bio {
			--hero-logo-height: min(22vh, 9.5rem);
		}

		.hero {
			min-height: calc(100svh - 8.5rem);
			padding-top: var(--space-md);
			--hero-skyline-gap: 1.25rem;
		}

		.hero__marks {
			flex-direction: column;
			justify-content: flex-start;
			gap: 1rem;
		}

		.hero__lung,
		.hero__wordmark {
			max-width: min(100%, 22rem);
		}
	}

	@media (prefers-reduced-motion: reduce) {
		.home-bio,
		.home-bio.is-pending,
		.home-bio[data-visible='true'] {
			opacity: 1;
			transform: none;
			transition: none;
		}
	}
</style>
